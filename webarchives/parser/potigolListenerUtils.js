var codigo = [];
var erros = [];
// Metodos uteis da implementação
function escreveCodigo(mensagem){
    codigo.push(mensagem);
}
function escreveErro(mensagem){
    erros.push(mensagem);
}
function exibeErros(mensagem){
    error.forEach(function(element) {
        console.error(element);
    }, this);
}
function exibeCodigo(mensagem){
    codigo.forEach(function(element) {
        console.out(element);
    }, this);
}
// Interpretador

function Escreva(mensagem){
    m   
}